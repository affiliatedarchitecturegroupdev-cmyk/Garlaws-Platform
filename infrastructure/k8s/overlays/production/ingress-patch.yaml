apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: garlaws-ingress
spec:
  rules:
  - host: app.garlaws.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: garlaws-app
            port:
              number: 80